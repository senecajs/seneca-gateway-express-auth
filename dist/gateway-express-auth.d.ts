declare function gateway_express_auth(this: any, options: any): {
    name: string;
};
declare namespace gateway_express_auth {
    var defaults: {
        cookie: {
            name: string;
        };
    };
}
export default gateway_express_auth;
